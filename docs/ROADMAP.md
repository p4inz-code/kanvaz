# Kanvaz — Roadmap: v4.3 → v5.3 (final stretch, closed) + v6.x (the real final arc)

*Living doc. Decided jointly (product judgement: user; technical scoping: assistant) — originally 2026-08-20, re-planned 2026-09-02, 2026-09-03, and 2026-09-04 (v5.3.0's close-out), reopened again 2026-09-04 for the v6.x arc below.*

**Everything from "Shipped — v4.3.0 through v4.6.1" down to "v5.3.0 — Finish line" is historical record — that arc genuinely finished and shipped as planned.** What follows this note is the NEW, currently-active plan: a full market analysis against PureRef and every other major competitor in this space (Milanote, Are.na, Eagle, Kosmik, ArtDeck), aimed at closing every real remaining gap in one considered, deliberately final push. v5.3.0's "last release" framing was accurate for the decision made at that moment, not a promise broken by this — the user chose to reopen development with a bigger, more specific target in mind. See "The v6.x arc" section, below the historical record, for the current plan and status.

## Framing

This is the last planned stretch of active feature development on Kanvaz — the user's own words: "these are the last versions of Kanvaz history." This supersedes the original three-version plan below: v4.3.0–v4.6.1 already shipped (ahead of and beyond what this doc originally scoped — MCP Bridge, whole-app MCP access, and a critical `.pur` fix all landed as user-driven mid-course additions, not part of the original plan). What's actually left is scoped as **four feature releases (v5.0.0–v5.2.0, plus v4.9.0's own remaining items folded into v5.2.0) and a fifth, final polish/future-proofing pass (v5.3.0)** — the user's own framing for how this stretch ends.

Standing constraint carried through all of it: **100% offline core, no accounts, no telemetry.** Any network-touching feature must be either a separate opt-in plugin (off by default) or, as of v5.0.0, an individually-disclosed opt-in-per-click exception in core (see SECURITY.md) — never automatic, never silent, never baked in as a background behavior.

---

## Shipped — v4.3.0 through v4.6.1

Full detail in `CHANGELOG.md` and `docs/HANDOFF.md`'s "Where things stand technically" section. Summary:

- **v4.3.0** — Command Palette, plugin `registerCommand`/`.on(event)`, Runtime Data API.
- **v4.4.0** — MCP Bridge (flagship plugin), real per-plugin permission enforcement, CI plugin packaging, Browse Official Plugins, Load unpacked plugin.
- **v4.5.0** — MCP Bridge widened to whole-app access (30 tools) — not originally planned, added mid-session by request.
- **v4.5.1** — CI release-build fix (electron-builder macOS hard-link bug) — no app changes.
- **v4.6.0** — Text card type, Map View inline rename + hover preview, bigger resize handles, critical `.pur` import hang fix, auto-updater confirmation flow, a dozen other real bugs fixed (undo snapshot aliasing, viewport restore, media leaks, annotation position/flip, zoom stepping, and more).
- **v4.6.1** — `.pur` importer fixed against a *real* PureRef file (v4.6.0's fix only covered synthetic test fixtures): multi-format detection (PNG/JPEG/GIF/BMP), corrected header-offset assumption, correct item/image interleaving, and a grid-fallback safety net when exact positioning can't be recovered.

---

## v4.7.0 — Organize & Connect

**Goal:** everything about seeing and managing a growing board — the areas users are actually asking about, Map View and card organization, tackled together since they're thematically the same problem (navigating many cards at once).

- **Board View: card renaming** — mirrors Map View's existing double-click-to-rename (shipped in 4.6.0). Currently the card-bar name is a plain non-editable `<span>` (`cards.js`) — confirmed gap, not assumed. Goes through the same `updateCardData()` path everything else uses, so undo/dirty-flag/MCP Bridge stay in sync automatically.
- **Map View: search/filter** — find a card by name/tag/type without leaving Map View.
- **Map View: multi-select + bulk actions** — marquee or shift-click to tag/delete/move several nodes together.
- **Map View: color-code nodes by tag or type** — visual grouping at a glance.
- **Tag chip resize polish** — tag chips are a flat `9px` in `main.css`, completely unresponsive to card size, unlike the video/audio scrub-time label right next to them (`clamp(10px, 3.5cqw, 16px)`). Give tags the same proportional treatment.
- **Video/audio control resize polish** — the 140px/200px hide-thresholds are instant `display:none` cliffs, not smooth. Replace with a transition.
- **Grid zoom-range increase** — the dot-grid background has limited range and disappears when zoomed way out. Not yet investigated; start with `resizeGrid()` in `canvas.js`.

**Done bar:** `validate.js` + `format-roundtrip-test.js` clean. Real Electron-GUI manual smoke test of every item above (this sandbox can't run the app — needs a human pass). No regressions in existing Map View/card-resize behavior.

---

## v4.8.0 — `.pur`: Full Round-Trip

**Shipped, partially — scoped down under real time pressure (3 releases requested in one sitting).** Only "Broader format detection" (WebP) and "Folder-drop auto-arrange" actually landed in v4.8.0. Export, better fidelity, TIFF, and z-order preservation are all still open — carried forward to a genuinely dedicated future `.pur` pass rather than rushed, per `CHANGELOG.md`'s 4.8.0 entry. Original goal below, unchanged, since it's still the right goal — just not what v4.8.0 itself delivered.

**Goal:** a dedicated release for the killer feature, not a few bullet points buried in a mixed release. `.pur` import already works against real files (v4.6.1) — this is about making the whole PureRef relationship excellent, not just functional.

- **Export back to `.pur`** — true round-trip compatibility with PureRef, not import-only.
- **Better import fidelity** — groups, rotation, exact scale (the current parser only recovers flat position/scale for individual images).
- **Preserve exact z-order on import** — the parser already extracts `zLayer` and sorts by it; wire it through to actual card stacking on create.
- **Broader format detection** — WebP and TIFF, beyond the PNG/JPEG/GIF/BMP v4.6.1 added. Live-test against real files with each format before calling it done, per the v4.6.1 lesson: synthetic fixtures alone did not catch real format assumptions being wrong.
- **Folder-drop auto-arrange** — drop a folder of loose images and get the same grid-arrange behavior `.pur` import's own fallback path already has (direct code reuse from v4.6.1's grid-fallback logic).

**Done bar:** export → re-import round-trips cleanly. At least one real-world test file per newly-supported format, not just synthetic ones (the v4.6.1 near-miss makes this non-negotiable). `validate.js` clean.

---

## v4.9.0 — Closing the Drawer

**Shipped, partially — under real time pressure.** Only "Auto-update download progress" and "zoom to selection" (pulled out of the Utility ideas line below) landed in the numbered v4.9.0 release. The annotation-drift fix and the rest of this list are now folded into v5.0.0/v5.2.0 below rather than kept as a permanently-open v4.9.0 — see those sections for current status.

- ~~Annotation resize-drift fix~~ — **done, v5.0.0.**
- **Utility ideas** (from user's own "ease user pain" ask): recently-used tags for one-click re-add, remembered last-used size per card type, snapping/alignment guides between cards. → v5.2.0.
- **Old backlog, revisited**: color-card multi-swatch/palette mode, note markdown preview. → v5.2.0. (URL card completeness became the URL preview feature — done, v5.0.0.)
- **`[data-theme="light"]` CSS cleanup** → v5.2.0.
- **Plugin authoring docs + scaffold template** → v5.2.0.

---

## v5.0.0 — Annotation drift fix, URL/File card previews

**Shipped.** See `CHANGELOG.md` for full detail.

- **Annotation resize-drift fix** — strokes now store as 0–1 fractions of card size instead of absolute pixels; old saves self-migrate on load.
- **URL card: "Fetch preview" button** — one-click title/thumbnail fetch, the one disclosed opt-in-per-click exception to the offline-core promise (see SECURITY.md).
- **File card: type-specific icon** — extension-derived label (PDF/ZIP/DOC/etc.) instead of one flat folder icon.

**Done bar met:** `validate.js` clean. Manual Electron-GUI smoke test of both new preview features and the annotation-resize path still needed (this sandbox cannot run the app — see v5.3.0's smoke-test item, which covers this release too).

---

## v5.1.0 — Template gallery

**Shipped.** See `CHANGELOG.md` for full detail.

**Goal:** the free-template-gallery idea, finally scoped and shipped (raised mid-session, referencing [storyflow.so/templates/filmmaking](https://storyflow.so/templates/filmmaking) as a reference point) — an acquisition/onboarding hook: pre-built starter boards a new user can drop straight into an empty board, free.

- **Shape decision:** an in-app "Start from Template" option next to "New Board," not a separate website/landing page — reuses the same bundled-catalog pattern Browse Official Plugins already established, so it's the smaller, faster, more maintainable option and stays consistent with the rest of the app's UX.
- A handful of bundled `.kanvaz` starter files (by use case — filmmaking, game art, mood board, to start) shipped alongside the app, not fetched from a remote catalog — keeps this feature 100% offline, no network exception needed for it at all.
- Template picker UI: a simple grid/list dialog off the "New Board" button, each entry with a name + short description.

**Done bar:** at least 3 real starter templates, each opens cleanly as a real board. `validate.js` clean.

---

## v5.2.0 — Remaining backlog sweep

**Shipped.** See `CHANGELOG.md` for full detail.

**Goal:** every item still open from v4.9.0's original list gets a real pass, closing that list out for good.

- Recently-used tags, remembered card size per type, snapping/alignment guides
- Color-card multi-swatch/palette mode, note markdown preview
- `[data-theme="light"]` CSS cleanup — hardcoded selectors → luminance-derived
- Plugin authoring docs + scaffold template
- Bulk-tag-per-card undo batching (Map View multi-select still pushes one undo entry per card instead of one per batch — flagged since v4.7.0)

**Done bar:** every item above either ships or gets an explicit, reasoned "not doing this, here's why" in `CHANGELOG.md` — nothing just quietly disappears. `validate.js` clean.

---

## v5.3.0 — Finish line: bug bounty, fixes, and what's next

**Shipped.** See `CHANGELOG.md` for the full writeup. Goal was: nothing new starts here — verify, polish, document, stop — the fifth and final release of this stretch, and the last actively-developed version of Kanvaz for the foreseeable future (the user's own framing). Active feature development by p4inz-code ends here; further feature requests go through GitHub issues or direct email, not a continued release cadence.

**What actually happened, honestly scoped:**
- **Real audit/bug-hunt pass, done.** 8 parallel finder passes (correctness line-by-line, removed-behavior regression audit, cross-file caller/callee trace, reuse, simplification, efficiency, architectural-depth, and a CLAUDE.md conventions check — none applied, no CLAUDE.md exists in this repo) across the full v4.7.0–v5.2.0 diff. 8 real, confirmed bugs fixed before shipping — see `CHANGELOG.md`'s "Fixed (found by this release's own audit pass)" section for the full list, including a genuine regression the v5.2.0 theme cleanup itself introduced (light-theme pinned+selected card shadow) and a real data-corruption path (annotation migration math while Map View is active). 2 more findings (reuse/duplication, not bugs) were logged but deliberately left unrefactored — see CHANGELOG's "Known, deliberately not refactored" note.
- **NOT done, disclosed rather than silently skipped:** the two deferred `canvas.js` findings from the 4.2.1 audit (additive-zoom-step, viewport-clamp-at-extreme-pan) were not specifically re-verified this pass — the 8-angle bug-bounty above covered the v4.7–v5.2 diff, not a re-audit of pre-4.7 code untouched since. Also not done: a real manual Electron-GUI smoke test of the full v4.7.0–v5.3.0 arc end-to-end — this sandbox has never been able to run the app at all (confirmed by direct test throughout this entire stretch), so every fix in this release (like every feature in v5.0.0–v5.2.0) is logic-verified and `validate.js`-clean, not eyes-on-screen verified. This is a real, standing gap for whoever picks up a genuine bug report against this code going forward, not a claim that's being quietly dropped.
- Final version bump: done. Final CHANGELOG "what's next" entry: done — see CHANGELOG's v5.3.0 section.

**Done bar, honestly assessed:** `validate.js` + `format-roundtrip-test.js` clean — yes. Every card type and Map View feature manually smoke-tested — no, per the disclosed gap above. `SECURITY.md`/`README.md` accurate as of the actual final state — yes. No further versions planned after this one — confirmed, per the user's own instruction to close out development here.

---

# The v6.x arc — the real final push

Reopened 2026-09-04, after a full market analysis against PureRef, Milanote, Are.na, Eagle, Kosmik (a cloud-AI-heavy competitor that shut down in 2026 — a real cautionary tale, not a hypothetical), and ArtDeck. Goal, in the user's own words: beat PureRef at everything it does, add what nobody else has done well, and leave Kanvaz in a strong enough state that it needs no attention for at least 2 years. Three pillars plus a dedicated polish pass, each shipping as its own tested, audited release rather than one giant untestable change.

**Standing design rule for this whole arc:** any AI feature must ship with a real, complete off switch from the day it's written, not bolted on after. Nothing AI-related should touch disk or memory when that switch is off.

## Pillar "Live Reference" — beat PureRef at its own game

- **v6.0.0 — shipped.** Always-on-top now defaults to on; click-through + adjustable window opacity ("Reference Mode," T key) let you trace/color-match straight through the Kanvaz window into whatever's underneath — PureRef's actual signature move, finally complete. Top Mode removed entirely (redundant once always-on-top is the default). See `CHANGELOG.md`'s 6.0.0 entry for full detail, including the settings migration that applies the new default to existing users too, disclosed rather than silent.
- **v6.1.0 — shipped.** Measure tool and eyedropper added to the annotation toolset; video frame-stepping (~1/30s steps) and onion-skin ghosting for checking timing/spacing. See `CHANGELOG.md`'s 6.1.0 entry.
- **Not yet done, and possibly out of scope:** pinning on top of one specific app only (PureRef 2.0's newer trick — flagged from the start as needing real OS-specific focus-tracking code per platform; may end up staying out of scope if it proves too fragile to trust versus the plain always-on-top already shipped). This is the one remaining open item in this pillar.

## Pillar "Never Lose Anything Again" — search & organization

Build order is safest/most-isolated first:
1. **v6.2.0 — shipped.** Smart Folders (a saved search that keeps re-running itself) and color search across image/color cards, both built directly on the existing Board View search with no architecture change. See `CHANGELOG.md`'s 6.2.0 entry, including two disclosed scope boundaries (video cards skipped for color search; Map View's own search doesn't get either feature this pass, deliberately not adding to its already-flagged duplication of Board View's search logic).
2. **v6.3.0 — shipped, with a real architecture pivot along the way.** Local AI search shipped as "Smart Search": lemmatized/fuzzy text matching via `wink-nlp` (pure JS, zero native dependencies), not the originally-planned transformer model — `@xenova/transformers` was tried first and reversed before anything was committed, once it turned out to require `onnxruntime-node`/`sharp` (native binaries needing Electron-ABI rebuilds this project has no CI for). Off by default; the worker only spawns when explicitly enabled. See `CHANGELOG.md`'s 6.3.0 entry for the full story and a real bug-bounty pass (6 findings, all fixed) before shipping.
3. **v6.4.0 — shipped.** Cards shared across boards within one file (the Are.na-style "same card, no duplicate, edit once updates everywhere" trick) — the hardest, most invasive piece, saved for last in this pillar specifically so it got the most scrutiny before shipping. Added a `sharedCards` registry to the save format (fully additive, no migration needed for old files) and caught a genuinely severe bug in its own pre-ship review: undo/redo was silently corrupting any shared card by snapshotting the save-file's content-less stub instead of the card's full live state. See `CHANGELOG.md`'s 6.4.0 entry for the full writeup.

**This closes out the "Never Lose Anything Again" pillar.**

## Pillar "Wide-Open Plugin Ecosystem"

**v6.5.0 — shipped.**
- A richer plugin API, fully additive: `createCardFromData` (insert any card type, not just one a plugin registered itself — what makes a template importer possible at all), `shareCardToBoard`/`unlinkSharedCard` (v6.4.0's shared-cards mechanism, now plugin-reachable), `showToast`/`showConfirmDialog` (so a plugin's own UI matches Kanvaz's instead of reinventing it), and the community-templates catalog fetchers. `kanvazApiVersion` deliberately stays at `1` — it's an exact-match gate for a real breaking change (see `plugin-loader.js`), not a feature counter, same precedent 4.3.0 already set; every existing plugin (`theme-creator`, `mcp-bridge`) keeps working completely unchanged.
- `docs/PLUGIN_AUTHORING.md` rewritten with all of the above plus an explicit, considered "Selling your plugin" section: yes, sell it wherever you want, at whatever price — Kanvaz will never add in-app payments or a marketplace (would put a solo-maintained project on the hook for refunds/fraud/tax forever), and a real constraint is disclosed rather than glossed over (a plugin's own network calls are subject to the same page-wide CSP as Kanvaz's own renderer, so a phone-home license check won't work — sell it as a one-time download instead, same trust model as any other paid desktop tool bought off itch.io).
- **Template Maker & Manager**, a new official plugin (`official-plugins/template-maker/`): save the current board as a template, manage your own collection (rename/delete/insert), and browse/install community-submitted ones via a new static `community-templates/catalog.json` in this repo — same free, no-server pattern "Browse Official Plugins" already established, with a PR-based submission process (see `community-templates/README.md`) rather than ongoing manual curation.

## Final phase — UI polish

**v6.6.0 — shipped. The v6.x arc is complete.** A full pass over every screen/control added by this whole arc, checked for CSS-variable usage (theme correctness), spacing/typography consistency, and hover/transition parity with equivalent existing controls. Result: the arc held up well — a repo-wide sweep for a raw hardcoded color outside an actual color-picker/swatch context found exactly one hit (a toggle switch missing its transition animation), fixed. See `CHANGELOG.md`'s 6.6.0 entry for the full audit writeup, including the one standing disclosed gap (no interactive Playwright/`_electron` driver exists for this project — every verification this whole arc did was a code audit plus a boot-test, never a live click-through).

**All three pillars — Live Reference, Never Lose Anything Again, Wide-Open Plugin Ecosystem — are shipped, tested, and documented.** This is the plan (see the top of this section) reaching its stated end.

**v6.6.1/v6.6.2 — real bugs slipped through even this polish pass.** A user reported Reference Mode throwing an error right after v6.6.0 shipped — it turned out the whole feature had been non-functional via its own button/shortcut/palette entry points since v6.0.0 (a scoping mistake: its code lived in the wrong internal module). Fixing that (v6.6.1) left one stale reference behind, breaking the Escape-key exit from click-through too — badly enough that a user could get stuck unable to click or close the app (v6.6.2). Both found and fixed via a real live reproduction against the running app over the Chrome DevTools Protocol, not a static read-through — see `CHANGELOG.md`'s 6.6.1/6.6.2 entries. The lesson stands exactly as disclosed throughout this arc: a boot-test is not the same as clicking the button, and it's now a standing requirement for the v7.x line below, not just this arc's own retrospective.

---

# The v7.x line — side-project continuation

*Opened 2026-09-13.* The v6.x arc's "genuinely done" framing (v6.6.0–v6.6.2) stands as an honest record of that decision — this isn't walking it back. The user has chosen to keep developing Kanvaz anyway, explicitly as a side project now rather than the main focus: "we will keep adding things to Kanvaz, keep developing it with feedback and new ideas." No fixed deadline, no "final arc" framing this time — ship items below whenever there's time, in whatever order makes sense, driven by real feedback as it comes in (like the Reference Mode bugs were). This section is the plan for a specific batch of feedback the user gave on 2026-09-13; it isn't a promise this is the last batch.

## Items, roughly in suggested order

1. **Resize modifier-key semantics — v7.0.0, shipped.** Swapped to match convention: free resize by default, Shift locks proportions (was backwards — aspect-locked by default, Shift freed it). Disclosed as a deliberate behavior change, same discipline as v6.3.0's `alwaysOnTop` default flip. Also found and fixed a real related bug: the aspect-lock exclusion for note/audio/url/file/text cards was only checked in one of two code paths in `startResize()`, so those types could still get corner-aspect-locked despite the exclusion. Verified live via CDP-simulated mouse drags (see `CHANGELOG.md`'s 7.0.0 entry), not just a static read.

2. **Video/audio control polish — v7.0.0, mostly shipped.** Play/pause and frame-step/onion-skin icons redrawn onto the same 16x16 viewBox grid the mute/loop icons already used (previously mixed 10x10/14x14, visibly inconsistent sizing in the same toolbar). Added a real per-card volume level slider (0–1, persisted), not just mute on/off. **Not done yet**: a small expand/fullscreen affordance — still open, low priority, fold into a future pass.

3. **MCP Bridge, made flagship-level — v7.1.0, shipped** (not originally on this list — added mid-arc after direct user feedback). 4 new tools (Reference Mode click-through/opacity, shared-cards-across-boards), 2 real schema-drift bugs fixed (`updateCard` silently missing `properties`, `updateSettings` still offering a setting removed in v6.0.0 while missing two real current ones). Also found and fixed a real, separate, much bigger bug while auditing this plugin's distribution: `official-plugins/catalog.json`'s `downloadUrl` for Theme Creator and MCP Bridge had been pointing at their original v4.4.0 release assets this whole time, even though CI rebuilds a fresh zip for every official plugin at every tagged release — "Browse Official Plugins" had been silently serving years-outdated builds. Fixed, but flagged as a standing manual step (see `docs/HANDOFF.md`'s v7.1.0 entry) until a real CI automation closes it properly.

3. **PDF preview for file-reference cards — v7.2.0, shipped.** A `.pdf` file-reference card now scrolls/zooms/page-navs right inside the resizable card, via pdf.js vendored as ~1.7MB of runtime files in `src/vendor/pdfjs/` (not the full ~35MB npm package). Found a real compatibility gap: Electron's bundled Chromium lacks JS features (`Promise.withResolvers`, `Iterator` helpers) pdf.js assumes — fixed with a targeted polyfill on both the main thread and inside pdf.js's own Worker (separate global scope). Every other file type (zip, exe, unrecognized binaries) still has nothing sensible to preview and keeps the icon-only card — scoped to PDF only, as planned.

4. **Annotation tool upgrade — v7.3.0, mostly shipped.** Ellipse, line, highlighter, and a real text tool joined pen/arrow/rectangle/measure/eyedropper; a custom color picker + session-scoped recent-colors row replaced the fixed-swatch-only palette; every stroke now has its own opacity (highlighter hard-clamps to max 35%/min 10px regardless of the slider). Found and fixed a real bug while building it: pen/highlighter used to re-composite the ENTIRE stroke path on every mouse-move frame, invisible at the old fixed 100% opacity but visibly darkening once strokes could be translucent — fixed by drawing one segment per move instead of the whole cumulative path.
   - **Not done yet — deliberately its own pass**: **select, move, and delete an individual existing stroke.** Today, once a stroke is drawn, the only way to change anything is "Clear annotations" (nukes everything) or undo (rolls back the whole board's last action, not stroke-scoped). This is the single biggest remaining quality-of-life gap versus Figma, and the part of this item that actually touches the stroke data model rather than just adding new draw tools — that's exactly why it was scoped out of v7.3.0 rather than folded in.

5b. **`.blend`/`.mb` 3D format support — researched, NOT built.** User asked whether Kanvaz's 3D preview could also open native Blender (`.blend`) and Maya (`.mb`) files directly, the way it opens `.glb`/`.gltf`/`.obj`/`.fbx`. Researched, not attempted:
   - **`.blend`**: Blender's own file format is undocumented/reverse-engineered (no official public spec) and version-dependent (the binary layout has changed across Blender's own major versions). The one community pure-JS parser, [`js.blend`](https://github.com/acweathersby/js.blend), is a small, single-maintainer, apparently-dormant project — not something to vendor into a production app the way Three.js itself was (a widely-used, actively-maintained library with a real security/maintenance track record). Fails the same bar `wink-nlp` was chosen against for Smart Search and the reason `@xenova/transformers` was reversed in v6.3.0: a fragile, thinly-maintained dependency for a feature this important.
   - **`.mb`**: Maya's binary format has no public specification at all and no known browser-side JS parser exists anywhere — Autodesk's own SDK only reads/writes it via a native C++ API. There's nothing to vendor; this would mean writing a binary format parser from scratch with zero reference implementation, a multi-month undertaking with no guarantee of correctness across Maya versions.
   - **Recommendation given to the user**: both Blender and Maya export `.glb`/`.fbx`/`.obj` natively in one click — these are the actual interchange-format standard for exactly this reason (every DCC tool supports them, no tool's proprietary format does). Kanvaz already supports all three. Not revisiting this without a genuinely different approach (e.g. a local conversion step via the user's own installed Blender/Maya, which is a very different, much bigger feature than "just add a format").

5a. **3D preview fixes — v7.5.0, shipped, same day as v7.4.0.** Direct user feedback on the newly-shipped feature: textures weren't loading (a CSP `connect-src` gap silently blocked every embedded-texture `fetch()` — see CHANGELOG v7.5.0), wireframe/matcap crashed on any multi-material mesh (routine in real exports, not an edge case), and orbiting/zooming inside the viewport also dragged/zoomed the whole board underneath it (the viewport never stopped event propagation). All three fixed and verified live. Also fixed: a NaN camera for zero-mesh models, and a GPU-resource leak on renaming a 3D card. **Reference Mode (click-through + window opacity) was removed entirely** in the same release, at the user's explicit request, after it was reported non-functional with no way to turn back off — a scoped attempt to fix it in place came first, but the user decided to cut it rather than keep a half-working flagship control. See `docs/HANDOFF.md`'s v7.5.0 paragraph before touching anything Reference-Mode-shaped again.

5. **3D model preview — v7.4.0, shipped. Kanvaz's 5th flagship feature.** New `model3d` card type, embedded like `image`/`video`/`audio` (not file-reference like `.pdf`) — a deliberate "reference boards stay self-contained" call. `.glb`/`.gltf` via `GLTFLoader`, `.obj` via `OBJLoader`, `.fbx` via `FBXLoader` (shipped, disclosed as best-effort per the original plan) — all three working, 150MB cap. Three.js 0.186.0 vendored (~2.5MB across 11 files) rather than the full npm package. Shipped controls: mouse-drag orbit (`OrbitControls`), Normal/Wireframe/Matcap render modes (exact-file rendering + a shading toggle — the (a)+(b) option the user picked over full custom-texture support, which is intentionally left for a future plugin), background-color swatch, reset-view, animation play/pause + scrub bar for models with clips. Render-on-demand (no continuous rAF loop for idle cards) and full GPU-resource disposal on delete, including fixing a pre-existing video/audio decoder-release gap in single-card delete found along the way. Camera orbit position is NOT persisted — every load reframes to a default view, a disclosed trade-off. UI is deliberately plain/functional; the user is designing the real UI in Figma separately. Found and fixed one real vendoring bug live (missing `three.core.js` — see CHANGELOG v7.4.0) and one real resource-leak edge case in self-review (dispose-registration timing). See `CHANGELOG.md`'s v7.4.0 entry for full detail.

6. **Additional supported formats, lower priority than the above.** Given Kanvaz's VFX/3D audience specifically:
   - **Fonts** (`.ttf`/`.otf`) — sample-text preview, cheap to build
   - **HDRI/EXR** — genuinely on-brand for lighting/reference HDRIs, needs a pure-JS EXR decoder plus exposure/tone-map controls; meaningfully bigger lift than fonts or PDF
   - Markdown/code file preview — moderate value, low cost, not requested directly but a natural extension of the PDF-preview work in item 3

7. **General UI polish** — explicitly "not blocking but great to have" per the user's own framing. No fixed scope yet; revisit after the items above land, the same way v6.6.0's polish pass happened after its pillars were functionally complete rather than alongside them.

---

# The v8.x line — industry-grade pre-production platform (planning, not started)

*Opened 2026-09-15. Not yet approved for execution — the user's own words: "no execution until i explicitly says go to execute." The user has also explicitly said this plan will go through two or three more full planning rounds before a start decision — this is round 2 of an expected multi-round process, not a near-final draft. Everything below is a proposed plan for review, not a commitment.*

## Framing

A deliberate pivot from "ongoing side project, whatever feedback comes in" (the v7.x line's framing) to a targeted push: make Kanvaz the pre-production standard for three specific professional groups — VFX artists, game developers, and animators — specifically the mid-to-senior, experienced end of each, not hobbyists. The user's own framing: these are the people who "need proper pre-production," and pre-production tooling is where creative directors and producers spend real budget and attention. Success bar, as stated: "every or most of these groups will use Kanvaz for pre-production."

This does not touch the v7.x line's own standing constraints (100% offline core, no accounts, no telemetry) — those carry forward unchanged. What changes is ambition and audience focus, not the app's fundamental privacy/offline posture.

**Elevated to a top-level, personal-not-just-technical constraint (2026-09-15): no cloud, ever, for Kanvaz's own core.** The user's own words: "privacy matters more than portability for me as a dev." This is not the same statement as "100% offline core" above, though it agrees with it — it's a stronger, personal line: no Kanvaz-run backend, no forced account, no sync service, period, regardless of how much easier it would make the team-collaboration features competitors like Miro/Soleil Clusters/Kosmik build their whole product around. AI integration and other opt-in internet calls (more MCP/agent capability, a fetch-preview-style feature, and so on) remain explicitly fine — the line is specifically about Kanvaz operating or depending on its own cloud infrastructure for the core product, not about network calls in general, which the v7.x line's own standing constraint already disclosed and scoped (individually opt-in, never automatic). **The one carve-out**: if a specific paying customer commissions cloud support as custom work, the user would consider it — but that's a bespoke paid-services engagement (see the enterprise/developer-support revenue stream, item 4 below), never a default feature of Kanvaz itself.

**Plan for getting there:** ship this batch as a real, audited release (or short sequence of releases), then the user personally emails working VFX/game dev/animation professionals to test it and give feedback — that feedback becomes the basis for the actual, final v8.x release. This roadmap section covers the pre-feedback build; the post-feedback iteration is explicitly out of scope until that feedback exists.

## Decisions made so far (2026-09-15 planning session)

1. **3D format support — tiered by actual technical risk, not treated as one undifferentiated "add more formats" ask.** Building this as reusable infrastructure rather than one-off per-format work, per the user's own "build once, no worries later" framing:
   - **Tier 1 — free wins, do regardless of everything else below. Shipped, 2026-09-15/v8.0.0-v8.1.0: `.stl`, `.ply`, `.vox`.** Three.js already shipped pure-JS loaders for all three (`STLLoader`, `PLYLoader`, `VOXLoader`), same vendoring pattern already used for `.obj`/`.fbx`/`.gltf` in v7.4.0 — self-contained single files, zero new architecture. **`.dae` (Collada) turned out NOT to be the same cost class — corrected here after actually checking, not assumed.** Three.js r186's own `ColladaLoader.js` is a thin 153-line wrapper around `collada/ColladaParser.js` (~2000 lines) and `collada/ColladaComposer.js` (~3000 lines), plus a `TGALoader.js` dependency for texture support — roughly 5,700 lines across 4 files of real COLLADA-XML scene-graph parsing complexity, nothing like STL/PLY/VOX's single self-contained file. Deprioritized pending a real decision: is 5,700 lines of harder-to-audit vendored parsing code still worth it for a format described as "older, still floating around some pipelines," or should `.dae` move to its own separate cost/benefit conversation rather than riding along as a "free win"?
   - **Tier 2 — SHIPPED 2026-09-15/v8.2.0: the USD family, via `USDLoader.js`.** USD is already the industry-wide interchange standard among exactly the studios this line targets (Pixar, ILM, and the rest of the Alliance for OpenUSD — Apple, Adobe, Autodesk, NVIDIA — all back it). Three.js's own `examples/jsm/loaders/` ships an official `USDLoader.js` (successor to the deprecated `USDZLoader.js` as of r179) that reads `.usda`, `.usdc`, *and* `.usdz` archives — vendored rather than a custom TinyUSDZ-WASM build, avoiding a whole new toolchain. **Corrected here, not glossed over: this was NOT the "collapses into Tier-1 cost" outcome originally hoped for.** Checking the real source before writing integration code (same discipline as the `.dae` check below) found `USDLoader.js` depends on `USDAParser.js`/`USDCParser.js`/`USDComposer.js`/`fflate.module.js` — ~10,100 lines across 5 files, not a single self-contained file like STL/PLY/VOX. Still the right call (official, actively-maintained Three.js code, no custom WASM engineering) — just a materially bigger, harder-to-audit vendored surface than Tier 1, and flagged for proportionally more scrutiny in the 3-session audit gate (item 2) as a result. Fidelity (complex material graphs, animation — USDLoader exposes no animation-clip extraction at all) is unverified pending live testing, not yet a known gap or known-fine.
     - Original WASM-path reasoning, kept as the fallback plan: a WASM module (e.g. TinyUSDZ-to-WASM, following the precedent of tools like needle-tools' USDZ viewer) is a materially different risk profile than the native N-API addons already rejected once for Smart Search (`onnxruntime-node`/`sharp`, reversed in v6.3.0) — it ships as a portable asset, no per-platform Electron-ABI rebuild, no `node-gyp`, no CI matrix burden.
     - **Legal/licensing, researched 2026-09-15 — no blocker.** OpenUSD (the format Pixar's own tooling, including USDC files, is built on) was open-sourced by Pixar in 2016 under a Modified Apache 2.0 License — the format itself isn't proprietary or copyright-encumbered against independent readers; file-format specifications don't carry copyright protection against compatible implementations (well-established, same reason any tool can read/write `.zip` or `.pdf` without a license from their originators). TinyUSDZ (the community C++/WASM implementation) is itself Apache 2.0 with some MIT-licensed helper components — both permissive, and Kanvaz already vendors Apache-2.0-licensed code (pdf.js) alongside MIT code (Three.js, Feather Icons) under the same `THIRD_PARTY_NOTICES.md` attribution discipline. A *specific* `.usdz` file a user drops in (e.g. an actual Pixar asset) carries whatever copyright its creator holds, exactly like any image or video card today — that's the user's content, not a Kanvaz format-support liability. Verify TinyUSDZ's and/or Three.js's `USDLoader.js`'s exact license file at actual vendoring time (standing convention, not skippable), but no reason expected to change this conclusion.
     - Scope v1 to static mesh + material preview either way; skip animation/skeletal data for the first pass.
   - **Tier 3 — PARTIALLY SHIPPED 2026-09-15/v8.3.0: `.blend` via Blender only.** `.blend` was already researched once and rejected (see the v7.x line's item 5b, above) — the only community pure-JS parser (`js.blend`) is dormant and single-maintainer, failing the same bar that got the transformer-model dependency reversed. `.mb`/`.ma` (Maya) has the identical problem: no public spec, no known JS parser. v8.3.0 ships the "optional external tool" abstraction for real, but Blender only: detect on a handful of known per-OS install paths (falling back to `PATH`), shell out to `blender --background --python-expr "...export .glb..."` (a real argument array via `child_process.spawn`, never a shell string — the security requirement locked in during the pre-go gap-check), render the result through the existing, unmodified Three.js pipeline. If not found, falls back to file-reference-only, matching the `.pdf`-card pattern. **Maya's `mayabatch`/`mayapy` invocation and Houdini's `hython` are explicitly NOT implemented** — deliberately scoped out rather than guessed at, since this session couldn't verify their exact CLI conventions with the same confidence as Blender's well-documented one. The abstraction is shaped so they can be added later without a redesign (same detect → convert → render pipeline), but that's future work, not done. A Settings-configurable tool path (for a non-standard Blender install location) is also not yet built — the candidate-path detection covers the common cases only.
   - **Tier 4 — elevated from "flagged for research" toward likely-necessary, given the "big industry houses" framing.** Alembic (`.abc`) is one of the actual standard interchange formats at large VFX studios specifically (camera/simulation/animation caches, used constantly between Houdini/Maya/Nuke pipelines) — not a nice-to-have at the audience this line is now explicitly targeting. Parser viability (pure-JS/WASM vs. requiring one of Tier 3's external tools to convert it) still hasn't been researched; that research just became higher-priority than a "someday" item, not resolved yet.
   - **Rejected — `.fla` (Adobe Animate/Flash source).** Asked and researched 2026-09-15: not worth it. Three problems stack against it, not just one: (1) pre-CS5 `.fla` is an undocumented proprietary binary format with no known parser, same class as `.mb`; even the newer (CS5+) `.fla`, while technically a zipped-XML package, still encodes full timeline/symbol/ActionScript application state, not just importable geometry or media — there's no "just the mesh" subset to extract the way glTF or OBJ has. (2) Flash Player itself was discontinued by Adobe in December 2020; the underlying runtime this format targeted is dead technology, and its remaining audience within VFX/game dev/animation specifically (cutout/2D animators using Animate) is a narrow slice even within an already-narrow use case. (3) A `.fla` is a working *source project* for an authoring tool, not reference material — it doesn't fit what a reference/mood board is for the way an exported render, spritesheet, or video clip would. If 2D/cutout animators want their Animate work referenced on a board, the natural path is exporting to something Kanvaz already handles well (video, GIF, or an image sequence), not opening the source file. Recommend: do not build this.
   - **All four tiers, plus the `.fla` rejection, are proposals — nothing here is approved for execution yet.**

2. **Build sequencing, decided 2026-09-15, refined further 2026-09-15: features → verify → polish everything → a 3-session audit gate → ship + legal — in that order, not interleaved.**
   - **Phase A — raw capability.** Build out every approved format tier and feature for this line before spending time on usability/UX polish. The user's own framing: "make sure v8 is full packed with tools and raw power," with "usability will be tackled after all features ship." This mirrors the v6.x arc's own precedent (its final UI-polish pass, v6.6.0, happened after all three pillars were functionally complete, not alongside them) — not a new pattern for this project. Along the way: **every version shipped before the audit gate below is a normal, official, numbered release (v8.0.0, v8.1.0, and so on) — "official but not industry-grade" is the user's own framing for this stretch.** These are real releases, not throwaway betas; they just aren't the flagship milestone yet. That milestone is earned at the audit gate, not declared by a version number.
   - **Phase B — verify, then polish on *everything*, not just what's new.** Refined 2026-09-15: the user's own sequence is "features, then verify, then polish on all features we have" — polish isn't scoped to only the new v8 work, it covers the whole app's existing feature set too, which is exactly what the "Polish existing" checklist (below) already anticipated.
   - **Phase assignment, decided 2026-09-15, resolving two open questions at once:** Frames/Sections, comment pins, and video in/out markers move into **Phase A** (raw capability), not left as general backlog — this round's domain-specific research gave them a specific, load-bearing reason to exist (VFX shot organization, creative-director review, reference-footage marking), which makes them core capability for this line, not optional polish. The "Polish existing" checklist's items split by their actual nature rather than being treated as one block: **camera-position persistence and individual annotation-stroke editing go into Phase A** (they're unfinished capability, not cosmetic — a professional hitting "Clear annotations nukes everything" isn't a polish complaint, it's a missing feature); **the video/audio fullscreen affordance and viewport-culling performance work fit Phase B** (genuine usability/performance polish, not new capability); **richer Properties field types (dropdown/date/number) go into Phase A**, since the game-dev asset-tracking workflow (item 7) actually depends on them functioning, not just looking nicer; **cross-board connections between independent cards stays a Phase A candidate, not a commitment** — still the single biggest lift in either list, and worth a real go/no-go call once the rest of Phase A's shape is known, rather than being decided in the abstract now.
   - **Phase C — a 3-session audit series, deliberately set as a high, strict pass gate, not a checklist to rubber-stamp.** The user's own words: "very high and strong pass gate." Three separate audit sessions, not one — mirroring (and going beyond) the multi-angle audit-pass discipline already established in this project's own history (the v5.3.0 finish-line pass, the v4.4.0 7-lens security review). Composition not yet decided (does each session cover different angles, or is each a full independent re-audit of everything?), but the standing bar carries forward regardless: real live verification via CDP (not a boot-test), `test/lint.js`/`test/validate.js` clean, and now real working professionals actually testing it (see "Plan for getting there," above) as part of what this gate requires before it's considered passed.
   - **Phase D — shipping, plus "legal."** Explicitly named as its own step by the user, separate from the audit gate. Scoped concretely now rather than left abstract: (1) Gumroad's own terms-of-sale/checkout are used as-is (not reinvented, per the website decision above) — the only writing needed is a short, plain-language description of what's being sold and its refund expectations, on the Gumroad listing itself; (2) a brief EULA-style note for the paid pack specifically (what you're licensed to do with a purchased template/plugin — e.g. personal/studio use, the already-decided peer-sharing norm — versus what crosses into redistribution/resale), since the MIT core license doesn't cover a separately-sold pack and shouldn't be stretched to imply it does; (3) enterprise/developer support engagements get their own simple written scope-of-work per engagement, not a fixed public contract, since pricing and scope are inherently per-customer (item 4). None of this needs a lawyer for a $10-20 digital pack at this scale — a clear, honest, plainly-written page covers it — but it should exist and be reviewed once, not skipped.
   - **Version numbering, decided 2026-09-15:** every release before the audit gate passes is a real, normal, numbered version (v8.0.0, v8.1.0, and so on as Phase A's format tiers and features land incrementally) — no special pre-release/beta tag needed, consistent with "official but not industry-grade" already being the user's own framing for this stretch. The distinction between "a v8.x release" and "the flagship, industry-grade v8 release" lives in the CHANGELOG/README narrative and in whether the audit gate has actually been passed, not in the version number itself — same pattern this project already uses to distinguish a real shipped release from a draft (a version number alone was never the thing that made something "done" in this project's own history either).
   - **Only after Phase C's gate is actually passed does the real, flagship v8.x version ship.** This sequencing is itself a decision recorded here, not just a description of normal process — the "official but not industry-grade" releases in Phase A are real steps along the way, not a dry run to be discarded.

3. **3D-first reordering — SHIPPED 2026-09-15.** Every place media-type order actually appeared got the lead-with-3D treatment: the empty-canvas drop-zone hint (`src/index.html`, "Drop a 3D model, image, video, or GIF here" — was "Drop images, GIFs or videos here"), the Import toolbar button's tooltip, and README's opening description, "Why Kanvaz" table, and Features list. **One thing corrected while executing, not assumed**: there is no "New Card menu" entry to reorder for images or 3D models — checked the actual canvas right-click menu and it only has "New note"/"New text"/"New color swatch"/"New URL reference"/"New file reference," since neither images nor 3D models can be created blank (they need real file content). The plan's own wording assumed a menu item existed that doesn't; corrected here rather than left wrong or invented. Command Palette has no media-type-order text to change either — checked directly, none found.

4. **Monetization: first-party paid pack, MIT stays.** License stays MIT — this is not a relicensing decision. On top of that, Kanvaz itself (not just third-party plugin authors, who already have this right per `docs/PLUGIN_AUTHORING.md`'s "Selling your plugin" section) will sell an official pack of paid plugins and exclusive templates, one-time lifetime price, low dollar amount. Explicitly floated as "just an idea" by the user — needs real scoping (what's actually in the pack, pricing, distribution mechanism given the CSP constraint already disclosed in `docs/PLUGIN_AUTHORING.md` that rules out a phone-home license check) before this becomes a committed plan, not just a decision to eventually do *something* here.
   - Open question this raises, not yet answered: does a paid official pack change the "Kanvaz never runs a marketplace, never takes a cut" promise already made publicly in the README and `docs/PLUGIN_AUTHORING.md`? That promise was about *other people's* plugins specifically — a first-party pack is a different claim and needs its own honest framing, not a quiet redefinition of the existing one.
   - The user's senior(s) are pushing for broader monetization of Kanvaz itself; the user's own read is to keep the core app as-is (free, MIT) and monetize via this narrower official-pack idea instead, with other monetization directed at the user's other, unrelated apps. Recorded here as the user's stated position, not something this doc is taking a side on.
   - **Distribution and quality bar, confirmed 2026-09-15: sold directly on Gumroad, by the user, as "a proper pro set of plugins and templates."** Answers the open question of what channel this goes through and what bar it needs to meet — a real, professional-grade paid pack, not a thrown-together bundle, sold from the user's own Gumroad storefront rather than any in-app store (consistent with the already-decided "no in-app marketplace, ever" promise — Gumroad is an external sales channel Kanvaz itself doesn't run or take a cut through).
   - **Peer sharing for supported/paying users, confirmed 2026-09-15: whoever has the paid/supported version can import and export profiles, templates, and plugins to and from each other directly, if they want to.** This extends the already-shipped profile export/import mechanism (a portable `.kanvazprofile` file, no network involved, since the v6.x arc) to also cover templates and plugins, specifically for people who've bought the pack — not a new subsystem, a broader scope for one that already exists. Consistent with the "no cloud, ever" constraint (above) and the same file-based-sharing spirit as item 8B's no-cloud review export: sharing happens by handing someone a file, never through a Kanvaz-run service. **Gating decided 2026-09-15: not technically gated — the same reasoning as item 9's piracy conclusion applies directly here.** A real technical gate (checking "is this a paid-pack file" before allowing export/import) needs exactly the kind of license/validation system item 9 already rejected as disproportionate and inconsistent with "no cloud, ever." The `.kanvazprofile`-style export mechanism stays what it already is — a plain file format, no built-in distinction between "bought" and "not bought" content. The norm is social, not enforced: this feature exists for people sharing what they've bought with a colleague, same as the peer-sharing framing already implies, and the same DMCA-for-real-abuse fallback from item 9 covers the case where that norm gets abused at scale.
   - **Second revenue stream added 2026-09-15: paid enterprise/developer support.** For a big studio/project that needs custom plugin development or hands-on technical support, charge for that support directly — a services/consulting revenue line, separate from the consumer-facing paid pack above. This is a well-precedented open-core-adjacent model (free MIT product, paid support/services for organizations that need it) — Kanvaz's own core stays free and MIT either way; this is selling the user's/Northbyte Studios' own time and expertise, not a licensing change to the software itself. Not yet scoped: what "support" concretely includes (SLA response time? on-call? custom plugin development as a paid service distinct from buying a pre-made plugin?), or pricing.

5. **Market/competitor research — started early at the user's explicit request (2026-09-15), superseding the original "hold until scope locks" plan.** A real, sourced pass, not a repeat of the v6.x arc's own prior analysis (PureRef, Milanote, Are.na, Eagle, Kosmik, ArtDeck) — this one specifically targets what's changed since and what's relevant to the pre-production/creative-director angle:
   - **Kosmik shut down.** Confirmed as of this research: Kosmik (a cloud-synced, AI-auto-tagging infinite canvas — a real competitor in the earlier analysis) closed new sign-ups and is telling existing users to export their data. This is a live, current displacement event, not a hypothetical — its former users are actively looking for a replacement right now. Concrete, time-sensitive opportunity if v8 ships while this is still fresh.
   - **A direct positioning threat: Soleil Clusters.** A newer entrant (`clusters.soleilpictures.com`) explicitly markets itself as a "creative workspace and moodboard for film, photo, design, and brand teams," and ranks itself for exactly one job: "reference boards a whole team works on together" — real-time editing, link sharing, cloud sync. This is close to the same production-team positioning this v8 line is aiming for. Its structural weakness, and Kanvaz's structural advantage: it's browser/cloud-based, not offline — the "100% offline, your files stay yours, no account, no subscription" pitch Kanvaz already makes is a genuine differentiator against it, not just marketing language, and should be leaned on hard in any v8 positioning.
   - **ArtDeck validates the one-time-purchase model.** A newer PureRef-alternative ($6, one in-app purchase unlocks everything permanently, no subscription) that also does alignment/distribution/snapping/z-order/groups/undo — a lot of overlap with what Kanvaz's Properties panel already does. Its pricing model is a real, working precedent for the "official paid pack, one-time lifetime price" idea already decided above — not an unproven approach in this exact market.
   - **PureRef is not stagnant.** Got a real 2.0 update (May 2024) and 2.1 (February 2026) — grid snapping, batch image processing, drawing tools, new language support. Still the dominant free baseline in this space; "beat PureRef" (the v6.x arc's own stated goal) means beating a tool that keeps shipping, not a frozen target.
   - **A separate, adjacent category exists: AI-assisted previs** (mStudio and similar), for 3D shot-blocking/camera planning specifically — distinct from moodboard/reference-board tools, not a direct Kanvaz competitor, but shows real budget and attention flowing into pre-production tooling broadly. Worth knowing about for positioning language ("pre-production platform") even though it's not the same product category.
   - **Not yet done**: this pass didn't dig into how creative directors and producers *specifically* (as opposed to individual artists) currently evaluate or adopt pre-production tooling at the studio level — budget-holder-side research, procurement patterns, what makes a tool "the standard" a whole department defaults to. That's the harder, more valuable research still open, and probably needs a different search strategy (industry publications, actual practitioner interviews/forums) rather than product-comparison articles.

6. **Post-v8: a real Kanvaz website, GitHub Pages, plus a creative live-stats display — decided 2026-09-15, explicitly scoped as AFTER v8 ships, not part of it.** This is the same website idea flagged and deliberately deferred multiple times before (see "Not a version, flagged so it doesn't get lost," below — that note previously called it "unscoped future work if the user chooses to pursue it"; this supersedes that with an actual decision and a trigger condition).
   - **Trigger:** only starts once v8 has shipped — not in parallel, not before. Sequenced deliberately after the "full pack of tools and raw power" work, same discipline as the rest of this line's sequencing decision above.
   - **Hosting:** GitHub Pages, giving Kanvaz its own dedicated page/domain (`p4inz-code.github.io/kanvaz` at minimum, or a custom domain if one gets attached later — not yet decided). Static hosting only, no server — consistent with the project's whole "no infrastructure to maintain" posture (same reason "Browse Official Plugins" fetches a static committed JSON instead of running a real catalog server).
   - **Purpose, stated explicitly by the user:** also goes into the user's own portfolio — this is a public-facing showcase piece, not just a landing page, and should be designed with that in mind (visual polish matters here more than it does for, say, an internal doc).
   - **Live download tracker, "in a more creative way" than what's in the README today.** The README currently uses flat shields.io badges (release/downloads/stars/license/platform/offline) — functional but generic, same badges any repo could have. Options worth considering when this actually gets designed (not decided yet, just scoped as the space to explore):
     - A custom-designed stats widget/card (GitHub-README-Stats-style) instead of a row of flat badges — same underlying data (release version, total downloads, stars), presented with actual visual identity instead of a generic badge service.
     - **A real technical constraint to design around, flagged now so it isn't discovered late:** GitHub's public API is rate-limited for unauthenticated requests (60/hour) — a live counter that every website visitor's browser queries directly against the GitHub API client-side risks hitting that limit under any real traffic. The safer pattern: a scheduled GitHub Action (e.g. every few hours) that fetches the real numbers server-side and bakes them into a static JSON/SVG asset committed to the repo or a `gh-pages` branch, which the website then just reads as a static file — "live" in the sense of regularly refreshed, not literally real-time per page load. This is the same shape of decision already made for `official-plugins/catalog.json` (a committed static file, not a live server) — worth reusing that precedent rather than inventing a new pattern.
   - **Not yet decided:** exact tech stack for the site itself (plain HTML/CSS/JS to match the main app's own dependency-light philosophy, vs. a static-site generator); whether it also serves as the distribution point for the future paid pack/enterprise-support offerings (item 4, above) or stays purely informational with links out to wherever those actually get sold.

7. **Domain-specific feature planning + competitive differentiation — this is the last planning phase before professional testing, per the user's own framing (2026-09-15): "v8 will be serious production, Kanvaz becomes flagship product of p4inz."** Researched three more competitors specifically (Refern, Miro, and the VFX previs-tooling landscape) to sharpen this rather than plan features in the abstract.

   **The single most important finding this round: Refern is the closest, most feature-rich competitor, and it has a specific, disclosed gap that lines up exactly with where Kanvaz already has a head start.** Refern is a local-first, one-time-$35, cross-platform (Win/macOS/Linux) reference tool with real depth — smart folders, hierarchical tags, color-hex search, image-to-image visual similarity search, 14+ search operators, pin-on-top with click-through. It is a genuinely strong product, not a weak one to dismiss. But: **Refern does not index 3D model files at all — `.blend`/`.obj`/`.fbx` are skipped entirely, and 3D preview is only "on the roadmap."** Kanvaz has shipped real, live, orbit-able 3D model preview since v7.4.0. This is not a minor feature checkbox — it's the exact wedge this whole v8 line's 3D-format push (item 1, above) drives into. Going deeper on 3D (USD, Blender/Maya/Houdini via the external-tool abstraction, Alembic) isn't just "more formats" in the abstract — it's the one dimension where the closest, most credible local-first competitor has explicitly not caught up and says so publicly. This should be a headline claim in v8's actual positioning, not a buried feature-list bullet.

   **Miro's weaknesses confirm what Kanvaz should keep NOT being.** Miro is expensive at scale (subscription, crippled free tier — only 3 editable boards), cloud-only, and a generalist whiteboard tool with images/video bolted on rather than built around creative asset types. It has zero native support for VFX-specific media (3D, real video/audio playback controls, PDF preview) and no offline story at all. Kanvaz's "100% offline, one-time-cost-or-free, built around real media types" identity is already the correct opposite of Miro, not something new to build — the differentiation here is entirely about saying it clearly, not adding features.

   **A real scope boundary, confirmed by researching the actual VFX previs landscape (mStudio, FrameForge, Previs Pro, Cinetracer, Storyboarder, Katalist AI, LTX Studio): Kanvaz should NOT try to become a previs/storyboard/camera-planning tool.** That market already has multiple dedicated, well-built tools across three tiers (AI-assisted, 3D-based, full-engine), and the actual professional workflow described in this research is "storyboard everything, reserve 3D previs for shots a drawing can't answer" — reference-gathering and organization happens as its own distinct, earlier step, which is exactly Kanvaz's lane. Chasing previs/animatic/camera features would mean competing in an already-crowded, differently-skilled market instead of owning the reference-and-organization layer that feeds into it. **Recommendation: stay a reference/moodboard/pre-production-organization tool, and go deep there — don't scope-creep into shot planning, camera simulation, or animatic timing.**

   **Domain-specific feature ideas, deliberately built from mostly-existing or already-flagged work rather than net-new invention** (the user asked to "plan each domain" — VFX, game dev, animation — separately):
   - **VFX**: Shot-based organization is the biggest gap — the already-flagged, not-yet-built **Frames/Sections** backlog item (a Figma-style grouping container with clipping) would let an artist organize references *per shot* within one board, a real VFX-specific workflow need. The already-flagged **comment pins** item serves the creative-director/producer review workflow directly — a director leaving notes on a specific reference is exactly the "budget holder" interaction this line's audience needs, and doesn't exist yet. The already-flagged **video in/out markers + filmstrip scrub** item matters specifically for VFX reference footage (marking the exact frame range that's actually relevant in a longer clip).
   - **Game dev**: Kanvaz's existing color search and tag/Smart-Folder system already cover a lot of "style guide consistency" and "asset status tracking" ground — this is more a *template and documentation* gap than a missing-feature gap (build a real template showing how to use existing custom properties + Smart Folders as an asset pipeline tracker, rather than new engineering). The already-flagged **persistent swatch library** backlog item would meaningfully strengthen this for palette/style-guide work specifically.
   - **Animation**: Already-shipped **onion-skinning** (video frame-stepping/ghosting, v6.1.0) directly serves timing-reference use today — likely under-marketed relative to its actual value for this audience. New need worth naming: character turnaround/expression-sheet reference is a template opportunity (a well-built bundled template), not a new feature — the canvas, connections, and tagging already support this workflow.

   **Paid template pack, now scoped against the above rather than as an abstract idea:** 2-3 flagship, genuinely deep templates per domain (VFX, game dev, animation), each built around a real professional pre-production workflow using the features above (existing + the newly-prioritized Frames/comment-pins/video-markers), priced in the $10-20 range per the user's own figure — not just the existing free starter templates re-skinned. What makes these "worth buying" has to be real workflow depth (e.g. a VFX shot-breakdown template that actually uses Frames-per-shot once that ships, not a generic moodboard with a VFX-themed color scheme).

   **Concrete starting proposal, 2026-09-15 — a real list, not just the abstract shape above** (final names/count stay the user's call):
   - **VFX**: "Shot Breakdown Board" (built around Frames/Sections — one frame per shot, references and notes inside each); "Director Review Board" (comment-pins-driven, pre-populated with example director-note threads so a buyer sees the workflow immediately, not an empty board).
   - **Game dev**: "Asset Pipeline Tracker" (Smart Folders + custom Properties status fields — the "template gap, not a feature gap" workflow, above); "Style Guide & Palette Board" (built around the persistent swatch library).
   - **Animation**: "Character Turnaround & Expression Sheet" (multi-angle/expression reference layout, consistency-check tagging); "Timing Reference Board" (built around the already-shipped-but-under-marketed onion-skinning/frame-stepping).
   - That's 2 per domain, 6 total — add or cut once Frames/comment-pins/swatch-library actually exist to build around, not just planned.
   - **Plugin ideas for the pack, more tentative than the templates above** — no domain research this round mapped as cleanly to a plugin concept: a batch export/rename utility (bulk-processing a folder of references, the kind of thing PureRef users have historically had to leave the app for), and a deeper Template Maker & Manager companion (versioning, personal-library sync via the peer-export mechanism already decided).
   - **Pricing**: individual templates ~$10 (the user's own lower bound), the full bundle at a real discount versus buying pieces separately (common bundle pattern; ArtDeck's own single $6 unlock-everything model is the closest local precedent). Enterprise/developer support stays unpriced here — inherently per-engagement, not a fixed shelf price.

8. **"What do most paid, best-selling tools not offer, that we give away free forever?" — asked explicitly 2026-09-15, proposed here, not yet decided.** Two concrete candidates, chosen specifically because they turn the "no cloud, ever" constraint (above) into an actual advantage instead of just a limitation to work around:

   - **A. An explicit "Free Forever" promise, stated as a real public pledge, not just an implicit fact about the current pricing.** Every card type, every file format Kanvaz ever adds support for (including everything from item 1's format tiers — USD, Blender/Maya/Houdini via the external-tool path, Alembic), the full plugin API, and unlimited use (no board-count or card-count caps, no usage-based feature gates) stay free forever in the core app — only the curated official template/plugin pack and paid enterprise support (both under item 4, already decided) are ever charged. This directly contrasts against a concrete, named competitor fact already on file: Miro's free tier caps users at 3 editable boards. Kosmik's shutdown is the sharper cautionary tale to invoke here — a cloud-dependent tool can simply disappear and take your organization with it; a promise phrased as "your boards are just files on your disk, forever, and the app that reads them never gets more limited" is a real claim Kanvaz can make and Miro/Soleil Clusters/Kosmik's own former users specifically cannot trust from a cloud tool. Precedent for this style of pledge exists elsewhere (e.g. Obsidian's "your notes are just local markdown files" promise) — not a novel format for making the claim, just not yet made explicitly for Kanvaz.
   - **B. A no-cloud async review/comment export — solving team collaboration without ever touching a server.** The gap: Miro, Soleil Clusters, and Kosmik all solved "a creative director needs to leave feedback on references" by building real-time cloud collaboration — an account and a live connection. PureRef, Refern, and ArtDeck (the local-first, single-user tools) don't solve this at all; there's no review workflow in any of them. Proposal: once comment pins ship (item 7, above), let a board (or just the commented subset) export as a single lightweight review file — something a producer or director can open, read, and leave comments on without owning a Kanvaz license or creating any account, then hand back to the artist, who re-imports it and the comments merge into the original board. This is genuinely different from what any competitor in this research offers: async, no-account, no-server team review, entirely file-based. Real open question this raises, not resolved: does opening/commenting on a review file require a free, separate "Kanvaz Viewer" mode/build, or can Settings gate this to keep it simple? Not scoped further than the idea itself yet.
   - Both are proposals for this planning conversation, not decisions — flagged as "do what's best" delegated judgment calls, but not treated as locked in without the user's own sign-off, same as every other item in this doc.

9. **Paid-pack piracy: recommendation, given a real question the user raised — "no key system usually, the risk is a buyer shares it with someone else."** Researched 2026-09-15 rather than guessed. Recommendation: **do not build DRM or a license-key/activation system for the paid pack. Accept casual peer-sharing as a cost of this specific business model, and rely on pricing + goodwill for the common case, legal recourse (DMCA) for the rare serious one.** Reasoning, not just an opinion:
   - **The core economic argument, confirmed by research:** the point of anti-piracy effort isn't absolute prevention, it's "raising the cost of piracy beyond the value of unauthorized use — a $50 tool won't justify months of reverse engineering, but a $5,000 tool might." A $10-20 template/plugin pack sits firmly in the range where building real protection costs more (in engineering time and, more importantly, friction for paying customers) than the piracy it would actually stop.
   - **A working license-key system needs server-side validation to mean anything** (client-only checks are trivially bypassed) — "validate the license on every app launch... server-side enforcement and device fingerprinting" is the documented pattern that actually works. That requires exactly the kind of Kanvaz-run backend the "no cloud, ever" constraint (above) already rules out on principle, independent of the cost/benefit argument. Building real DRM here would mean violating a constraint the user has called more important than portability, to stop a low-stakes problem.
   - **This isn't a new conclusion for this project — it already reached the same one for third-party plugin sellers.** `docs/PLUGIN_AUTHORING.md` already discloses that a plugin's CSP prevents a phone-home license check from working at all. Building covert enforcement into the *first-party* pack that the docs already told third-party sellers wasn't feasible would be inconsistent — hold the first-party pack to the same honest standard, not a quietly different one.
   - **Why this doesn't apply to the user's other, subscription-based app**: a subscription inherently requires recurring server-side authentication just to keep the product working (a lapsed subscriber loses access on its own) — license enforcement there is a natural side effect of how subscriptions already work, not an extra system bolted on. A one-time-purchase, offline, MIT-core product has no equivalent natural checkpoint, and adding one contradicts the product's own identity rather than fitting it.
   - **What's actually worth doing, all low-cost and consistent with the rest of this plan:** (a) price low enough and deliver enough real workflow depth (per item 7's "worth buying" framing) that most buyers have no reason to look for a pirated copy; (b) a light, non-blocking, inert deterrent — e.g. quietly embedding the buyer's Gumroad order reference in the downloaded files' metadata, purely as a traceability/accountability signal, never as something that blocks functionality or calls home; (c) for the one scenario that actually matters commercially — someone publicly re-uploading or reselling the whole pack, not a single friend-to-friend share — handle it the way every other indie seller does: a DMCA takedown against wherever it's hosted, not a technical fight. The already-decided peer-sharing feature (item 4, above) already accepts individual sharing as fine; this just extends the same logic to the rest of the pack rather than trying to inconsistently block it.

10. **Fresh market/audience research (2026-09-15), specifically on what the target audience needs and what current tools get wrong for them:**
    - **VFX art departments' actual workflow, confirmed:** "Art departments create mood boards, sketches, illustrations and key art frames to drive direction for other departments, and collaborate closely with asset departments through paint-overs on renders for design changes and reference photography" — this is a direct, sourced confirmation that Kanvaz's core moodboard/reference-organization use case is exactly the real workflow at a place like ILM, not a guess about what VFX pre-production looks like.
    - **PureRef's own disclosed weak points, reconfirmed with more specificity:** no built-in multi-device sync (a real gap for professionals who work across a studio workstation and a laptop — though notably NOT the same thing as needing cloud sync; a local network/manual-file-transfer story could address this without breaking "no cloud, ever"), no advanced editing/annotation tools (Kanvaz already has real ones — pen/arrow/rectangle/ellipse/highlighter/text/eyedropper/measure — re-confirms existing differentiation, not new work needed), and **occasional lag/performance issues with very large, high-resolution image collections.** That last one is a genuine, external validation of something already on Kanvaz's own backlog (viewport culling, listed under "performance" in the original remaining-backlog notes) — worth treating as a real competitive bar to clear for this audience specifically, not just a nice-to-have.
    - **Industry context, lower-confidence but worth knowing:** 2026 saw real VFX industry consolidation — studio closures, work migrating from mid-tier vendors toward Tier-1 majors. Not directly actionable, but it's the kind of environment where a genuinely useful, low-cost or free professional tool has more room to gain adoption than in a stable, well-funded market where studios already have established (often expensive, enterprise) tooling relationships.
    - **Not yet done, still the harder research:** the budget-holder/procurement-side question (how a creative director or producer at a studio actually evaluates and adopts a tool like this) remains unanswered — this round's searches kept surfacing individual-artist-level information, not organizational adoption patterns. Likely needs direct outreach (the planned professional-tester emails, item "Plan for getting there") rather than more web research to actually answer.

## v8.x Feature & Polish Checklist — consolidated, audited 2026-09-15

The items above are decision narrative, with reasoning and research attached — this section pulls the actual build list out of that narrative, split into **new** and **polish existing**, so it's something to scan rather than re-read. Nothing here is new information; it's the same decisions, organized for reference. A short audit pass over the whole v8.x line found and fixed one real numbering bug along the way (item 8A previously mis-cited "items 3 and 4" for the paid pack/enterprise support — both actually live under item 4 alone; corrected above).

**New — formats** (item 1):
- `.stl`, `.ply` (Tier 1 — vendor existing Three.js loaders)
- `.dae`, `.vox` (Tier 1, lower priority, same cost class)
- `.usd`/`.usda`/`.usdc`/`.usdz` (Tier 2 — evaluate Three.js's own `USDLoader.js` first, fall back to TinyUSDZ-WASM only if fidelity is inadequate)
- `.blend`, `.mb`/`.ma`, Houdini files (Tier 3 — one shared "optional external DCC tool" abstraction: detect on `PATH`/Settings-configured path, headless-convert to `.glb`/`.fbx`/`.abc`, render through the existing Three.js pipeline; file-reference-only fallback if no tool found)
- `.abc` Alembic (Tier 4 — research still needed on whether it's a pure-JS/WASM parse or another Tier-3-style external-tool case; elevated priority, not yet resolved)
- **Rejected, do not build:** `.fla` (Adobe Animate/Flash)

**New — features** (item 7, mostly promoting already-flagged backlog rather than inventing):
- Frames/Sections (Figma-style grouping container with clipping) — shot-based organization for VFX
- Comment pins — creative-director/producer review workflow
- Video in/out markers + filmstrip scrub — VFX/animation reference footage
- Persistent swatch library — game dev style-guide/palette consistency
- No-cloud async review/comment export (item 8B, proposal not yet approved) — file-based team review without an account or server

**New — business/positioning** (items 4, 6, 8):
- First-party paid template/plugin pack, one-time price, sold direct on Gumroad, with peer import/export for paid users (profiles/templates/plugins)
- Paid enterprise/developer support (services line, separate from the pack)
- Post-v8 GitHub Pages website + portfolio piece + redesigned live-stats display
- Explicit public "Free Forever" pledge (item 8A, proposal not yet approved)

**Polish existing — proposed 2026-09-15, not yet in any decision item above, added by this audit pass specifically because the user asked for "polishing current to the highest possible given our situation and principles":** these are real, already-*self-disclosed* gaps (README's own "Known limitations" section, and the v7.3.0 CHANGELOG entry's own words) that a genuine "flagship, industry-grade" release and real professional testers will hit quickly if left alone. None of these require new architecture or violate any standing constraint (no cloud, no native deps without audit, MIT core) — they're finishing work already scoped, not new scope:
- **3D preview: camera orbit position — SHIPPED 2026-09-15/v8.4.0.** Persisted via `OrbitControls`' `end` event, one history entry per gesture, same convention as `setRenderMode`/`setBgColor`. **Custom-texture support remains NOT done** — still a disclosed, deliberate trade-off ("custom user-swappable textures aren't supported yet, planned as a future plugin"), not yet revisited. Given v8 makes 3D the flagship identity (the Refern-gap positioning, item 7), being unable to swap a texture without a plugin is a sharper pain point now than it was as a launch-scope trade-off in v7.4.0 — still open, still worth a real look.
- **Annotation: select/move/delete an individual existing stroke.** Already named by the project's own v7.3.0 CHANGELOG entry as "the single biggest remaining quality-of-life gap versus Figma" — today the only options are "Clear annotations" (nukes everything) or a full board-level undo. A creative director annotating a reference for a professional tester is exactly the workflow this blocks.
- **Custom Properties: real field types (dropdown/date/number), not text-only.** `registerPropertyFieldType` is a disclosed, unimplemented plugin-API gap. Directly serves the game-dev "asset status tracking" workflow this planning round already identified (item 7) — a status field is far more useful as a dropdown than free text.
- **Video cards: an expand/fullscreen affordance — SHIPPED 2026-09-15/v8.5.0.** Flagged as "not done yet" since v7.0.0's own control-polish pass. Real `requestFullscreen()` on the video element. **Audio cards reviewed as part of the same pass, found already solid — no changes forced.** Also fixed while auditing this area: three separate places (`reference-types.js`, `plugin-api.js`, `map-view.js`) that were all missing a `model3d` entry since v7.4.0, and 3D cards missing the annotation-count badge every other type has.
- **Dialogs/notifications/templates audit — SHIPPED 2026-09-15–2026-09-16/v8.6.0–v8.8.0, fully complete.** Direct request: "verify everything, do real research and improve the templates and improve the restore dialog and improve all dialogues and notifications." Shipped across four releases: the recovery dialog reports real board/card counts and save time instead of a content-free "Restore it?"; every delete/reset/remove confirmation dialog defaults Enter to the safe button (a risky positional-fallback draft was caught and removed before shipping, then a full-repo re-grep in v8.6.1 caught two more of the same shape in `sidepanel.js`/`annotate.js`); video cards' scrub/tag bars switched from flat theme-colored slabs to a translucent blurred scrim; profile export/import verified end-to-end; a README "Workflows by domain" section added. **v8.8.0 closed the last piece**: a script-driven audit of all 14 bundled templates found them structurally clean but text/note/color-only with zero reference-media cards anywhere — every production-pipeline template now has a tailored "drop your references here" section naming the real media (3D model format included) that domain's own process notes assume exists.
- **Top Mode — SHIPPED 2026-09-16/v8.7.0-v8.8.2, reintroduced by direct request.** Removed entirely in v6.0.0 on the reasoning that always-on-top + auto-hide chrome becoming the app's own default made a dedicated mode redundant — brought back because "get everything but the canvas out of the way right now, then back exactly as it was" (including the side panel, which the v6.0.0 default never touched) is a real, distinct ask. `Ctrl+Shift+T`, session-only, drives the same functions the Settings checkboxes already call rather than a new persisted flag. **v8.8.1**: a 4-agent self-review found and fixed two real conflicts between Top Mode's temporary override and two other writers of the same settings (`applySettings()`, `toggleAlwaysOnTop()`) that didn't know Top Mode existed. **v8.8.2**: live CDP verification against a real running instance (the first for this line) found the badge/border indicators were invisible on the Home Screen (a z-index/stacking bug static review missed entirely) — fixed and re-verified with real screenshots. **v8.8.3**: the persona-based pass (full audit suite section 4) found Escape never closed the Shortcuts overlay while still silently deselecting cards — fixed and re-verified live. **v8.8.4**: direct hands-on user testing (not automated) added drag-to-scrub on Transform fields and fixed the context-menu-stuck-open and FPS-overlay-on-logo bugs — see the same testing round's much larger backlog of NOT-yet-done items below (templates rebuild, tags-to-Properties migration, presentation mode, Layers/Map-View/3D-nav work, pre-production positioning research). **v8.8.5**: a live screenshot caught the Properties-panel-not-refreshing bug in the act — fixed (see CHANGELOG for the isOpen()-vs-isSectionVisible() root cause); `.blend` conversion failures now fall back to a file-reference card like the not-installed case already did; Blender version-detection list extended 5→14 entries. `.ma`/`.mb` (Maya) confirmed still fully unimplemented, a separate unstarted item, not a `.blend` variant. **v8.9.0**: closed the tags-to-Properties-migration backlog item (direct, repeated request) — in-card tag bar deleted entirely, Properties panel is the sole tag editor, autocomplete ported as a native `<datalist>`; gave the Board View titlebar a real profile avatar matching the Home Screen's own; a fresh audit found and fixed the About/Shortcuts/Official-Plugins overlays being invisible on the Home Screen (same z-index-99998 bug class as Top Mode's), the Shortcuts overlay's own layout being too cramped once actually visible, a Mac Cmd+Scroll gap, and Mac shortcut-label display text. Backlog items still open below are unaffected — this pass deliberately scoped to what's listed as done here, not a rewrite of the rest.

- **Backlog opened by direct user testing of v8.8.3-v8.8.4, none started yet — deliberately not rushed:**
  1. Templates are still text/note-only — correctly called out as not a real professional pre-production starting point. Needs real research (reference real industry pipeline tools/templates) and a full rebuild, not another patch on top of v8.8.0's work.
  2. `.blend` drop reportedly errored without falling back to a file-reference card — needs a real repro and fix.
  3. ~~Move tag editing out of each card's own tag-bar entirely, into the Properties panel only, with sync working both ways.~~ **SHIPPED 2026-09-16/v8.9.0** — see the Top Mode item's own entry above for the fuller writeup.
  4. Properties panel needs a real, larger feature-scope plan (the user asked for this explicitly, not just bug fixes).
  5. Layers panel: labels, a "pin to highlight" separate from lock, Maya-style grouping.
  6. ~~3D model mode: a way to pan/recenter without losing zoom besides pressing F.~~ **SHIPPED 2026-09-16/v8.9.1** — Alt+drag (matching every other card type's existing "pan from anywhere" gesture) or Ctrl/Cmd+middle-mouse now pans the whole board even with the cursor over a 3D card's viewport, without disturbing OrbitControls' own plain-middle-mouse dolly. Live-verified: tx/ty moved by the exact mouse delta on both triggers, plain middle-mouse correctly left untouched, card does not get selected mid-pan (matching other card types' Alt-drag behavior). New `KanvazCanvas.startExternalPan()`, called from a capture-phase mousedown listener on the 3D viewport (capture is the one ordering that lets an ancestor pre-empt OrbitControls, which binds directly to the inner `<canvas>`).
  7. Map View node cards and 3D-model/video thumbnails need real preview/visual work, not placeholder icons.
  8. Presentation/kiosk mode for presenting a board from inside the app.
  9. Real research: positioning Kanvaz as a pre-production workspace tool specifically for 3D animation and game dev (2D under that umbrella), with an eye toward features that would pull users off Maya/Adobe tools specifically.
  10. Grid style feature (Reference/3D-origin/Game-Dev tile) questioned directly by the user's own stated "senior real feedback" — worth a real design review of whether tile grid earns its keep against reference mode.
  11. Performance/stability pass — reported jitter/lag on Ctrl+drag box-select specifically. **Investigated 2026-09-16, inconclusive — not closed, needs the user's own hands-on profiling.** Measured both plausible interpretations live via CDP against an 80-card stress-test board: box-select's `updateMarquee()` (canvas.js) averaged 0.017ms/mousemove-event (max 0.4ms, 60 events in 1ms total), and single-card drag WITH its O(cards) alignment-snap check (`findAlignmentSnap()`, cards.js — runs on every drag tick, not just box-select) averaged 0.078ms/event (max 0.4ms, 60 events in 4.7ms total). Both are well under 1% of a 16.7ms/60fps frame budget even at 80 cards — no measurable JS execution bottleneck in either code path. The reported feel is real and shouldn't be dismissed, but it's very unlikely to be fixable by optimizing this JS further, since the JS isn't where the time is going in this measurement. Most likely candidates left unexamined: real mouse/trackpad hardware sampling behavior, OS/GPU-compositor frame pacing, or a board with many real media cards (images/video, not the plain notes used here) where per-card *paint* cost (not JS logic cost) could differ. Needs the user to reproduce it while it's happening and check Chrome DevTools' own Performance tab (or just report which specific board/card-count/card-type triggered it) rather than a guessed code change.
- **Cross-board connections between independent cards (not the same shared card).** A real, disclosed limitation ("only one board's cards load at a time... As of 6.4.0, sharing the *same* card across boards... covers most of what people actually want this for" — but not all). Bigger lift than the others here; flagged as a candidate, not a commitment, since it may need real UI design work (a cross-board picker), not just a data-model change.
- **Viewport culling for large, high-resolution reference collections.** Already on the general backlog under "performance," now with real external validation (item 10, market research): large-collection lag is a specifically documented PureRef complaint from this exact audience, not a hypothetical Kanvaz-only concern — worth treating as a real competitive bar for this line, not just an eventual nice-to-have.
- **Not proposed for this pass, deliberately:** MKV/AVI playback (a Chromium codec limitation, not something Kanvaz's own code can fix) and per-profile plugin code isolation (previously declined as a multi-week rearchitect not worth it against current plugin-ecosystem size — nothing in this v8 push changes that calculus).

## Proposed features — noted 2026-09-16, none started, all need real scoping before a go/no-go

Five ideas from the user, recorded as given, explicitly NOT executed yet — each needs its own real design/feasibility pass before committing, not a rushed build:

1. **Vectorized annotations + selectable strokes** — convert the annotation tool from flat pixel canvas overlay to vector paths, so individual brush strokes can be box-selected/moved/resized/deleted like image cards. Real rearchitecture of `annotate.js`'s whole rendering model (currently `<canvas>` pixel drawing), not a small patch.
2. **"Ghost" Reference Mode — true click-through overlay** — a toggle making the window semi-transparent AND click-through (mouse events pass to whatever's underneath), so a 3D modeler can sculpt "through" a Kanvaz reference overlaid on their DCC viewport. Electron supports `setIgnoreMouseEvents(true, {forward: true})` for click-through, so technically feasible — but note this app's OWN "Reference Mode" (click-through + window opacity) was already built once, then **removed entirely in v6.5.0-era after the user reported it non-functional with no way to turn it back off** (see `docs/HANDOFF.md`'s v6.x history). Re-proposing this needs a read of that removal's reasoning first, not a clean-slate build — whatever broke it before needs a real answer this time.
3. **DCC plugin bridges (Blender/Maya/Unreal "send to Kanvaz")** — a companion Python plugin per DCC app with a one-click "send selected object" that exports and drops it onto the canvas. Real, separate codebases per DCC (Blender's `bpy` plugin API, Maya's own, Unreal's own) — three independent small projects, not one feature.
4. **Advanced media scrubbing** — frame-by-frame arrow-key stepping, A-B loop markers, and a playback-speed slider (0.25x/0.5x/...) for video cards. Playback speed already exists (`Playback speed` in the video context menu, per the current build) — this would be extending it with looping markers and frame-step, not starting from zero.
5. **Local peer-to-peer sync (WebRTC/local WebSocket)** — two machines on the same LAN/VPN seeing each other's canvas moves live, no cloud account, matching the project's own offline-first/NDA-safe positioning. Real multi-user conflict/merge design needed (this app's whole data model today assumes a single local editor) — not just a transport-layer add-on.

**Also flagged the same session, real and fixed same night**: `canvas.js`'s Ctrl+Scroll fine-zoom checked `e.ctrlKey` only, missing the Mac `e.metaKey` (Cmd) equivalent every other shortcut in this app already handles via `shortcuts.js`'s `ctrl = e.ctrlKey || e.metaKey`. Fixed. **Still open, not yet done**: the Shortcuts overlay (`?`) and Command Palette display literal "Ctrl+S"-style text even on Mac — the keys themselves work (Cmd triggers correctly), but a Mac user reading the help text sees the wrong modifier name. Needs a platform-aware label pass (`navigator.platform`/`userAgent` check → show `⌘` in place of `Ctrl` for display purposes on Mac) across both the Shortcuts overlay's list and `commands.js`'s `shortcut` fields — not done tonight, deliberately, given the scope (dozens of entries) and no live Mac hardware to verify against.

## Open questions (not yet answered)

Cleaned up 2026-09-15 — this round resolved most of what was here (website stack, template/plugin list, pricing, Refern positioning, the Free Forever hedge, the 8B viewer question, and the Phase A/B assignments); full reasoning for each lives in the decision items above, not repeated here. What's genuinely still open, because it needs either the user's own call or real execution-time information this planning pass can't resolve in the abstract:

- **Timeline for the professional-tester email round** — genuinely the user's own calendar decision, not something a plan can pre-decide. Practical default in the meantime: treat it as trigger-based ("when Phase C's audit gate passes") rather than a fixed date, since a calendar date set now would just be a guess.
- **Does the USD family (Tier 2) get its own release, or bundle with Tier 1/3?** — a real packaging question, best answered once Tier 1/2's actual implementation size is known, not before.
- **Alembic (Tier 4): who does the research, and does it gate the rest of the format work?** — still unresearched; whether it blocks or ships independently depends on what that research finds.
- **Cross-board connections between independent cards** — already flagged as a Phase A *candidate*, not a commitment (see item 2's Phase-assignment decision) — the actual go/no-go call is deliberately deferred until the rest of Phase A's shape is known, not left open by oversight.
- **Exact final template/plugin list and count** — a real starting proposal exists now (item under "Decisions," above), but the final list is the user's call, not something to lock in unilaterally.

## README audit findings — collect now, fix in one pass at the end of this run

The user asked to keep flagging README mistakes as they're found during this planning run, then do one full rewrite for v8 at the end, rather than patching piecemeal. Findings so far:

1. **"Prebuilt installers are Windows only" is factually wrong today, not just outdated wording.** Verified 2026-09-15 against the actual `v7.26.0` GitHub Release assets: CI already builds and publishes a real macOS installer (`Kanvaz-7.26.0-arm64.dmg`) and a real Linux one (`Kanvaz-7.26.0.AppImage`) on every single release, automatically, via the existing 3-platform build matrix in `.github/workflows/build.yml`. The README's "Download for Windows, it's free" CTA and its own note directly under it ("macOS and Linux users build from source") both undersell what already ships — this isn't a v8-only concern, it's actively wrong right now and has presumably been wrong across multiple past releases. Given the v8 line's audience (VFX studios in particular run Linux heavily in-pipeline), this also undercuts the exact cross-platform credibility this line is trying to build. Fix in the rewrite: CTA and installer section should reflect that Windows/macOS/Linux installers all already exist; "build from source" framing should be reserved for whatever genuinely still requires it (if anything).

## Final gap-check before a start decision (2026-09-15) — all six resolved

Asked directly by the user before going ahead — a real audit against what a plan this size typically misses. All six now have a decision recorded, at the user's explicit request ("yea les do em rn").

1. **RESOLVED — locked in as a firm requirement, not a proposal.** Tier 3's external-DCC shell-out (Blender/Maya/Houdini) is new attack surface shaped like a command-injection risk if built carelessly. Requirement, non-negotiable before any Tier 3 code ships: real path validation/sanitization, `child_process.spawn` with a real argument array (never `exec` with an interpolated shell string), and this mechanism gets its own dedicated pass in the Phase C audit gate — not folded into a generic look-over. Same bar the project already held itself to for the v4.4.0 7-lens security review and the plugin-install zip-slip guard, applied to this new surface.
2. **RESOLVED.** The Free Forever pledge (item 8A) explicitly includes: existing v7.x users get every v8 core improvement free, unconditionally, no re-purchase — the same free-upgrade path every prior version bump in this project's history has already been. Only the separately-sold pack and enterprise support ever cost anything, exactly as already decided; this just closes the "what about people already using it" gap in how the pledge was worded.
3. **RESOLVED, pending confirmation once real code exists.** Based on the project's own established precedent (`kanvazApiVersion` is an exact-match gate for a genuine *breaking* change, not a feature counter — v6.5.0 added a large amount of new plugin-reachable capability without bumping it, because none of it removed or changed existing behavior) — **no `kanvazApiVersion` bump is expected for this batch**, since everything planned (new card types/fields, the external-tool subsystem, new UI) is additive, not a change to any existing plugin-facing contract. This is a reasoned expectation from precedent, not a guarantee — whoever actually implements Tier 3 should re-check this against the real diff before shipping, the same way every past version's plugin-facing changes got checked.
4. **RESOLVED — formalized as a firm requirement.** Every new card field or structure this line adds (3D-format metadata, Frames/Sections' own data, comment pins) must be fully additive to the `.kanvaz` format, matching the unbroken precedent of every past addition (the container format itself, `sharedCards`, `mapPosition`). Old files keep opening; nothing silently migrates or breaks. Not a new rule — just no longer just assumed, stated as a real requirement for this line specifically.
5. **RESOLVED — researched, inconclusive, not a blocker, but real diligence recommended before heavy commercial investment.** A casual search found no exact "Kanvaz" (with a Z) trademark registration, but did find an existing "Kanvas" (with an S) trademark portfolio in adjacent software/collaboration space — including "Kanvas, LLC" specifically for collaboration software, a category near enough to a reference/pre-production tool to be worth taking seriously. A casual web search is not a real clearance search and cannot rule out a conflict either way; USPTO's own TESS database wasn't reliably queryable this way. **Recommendation, not a decision only the user can make alone: get an actual trademark clearance search done (a paid service or a real IP attorney, not another web search) before investing heavily in "official, industry-grade" marketing or filing for a trademark of Kanvaz's own** — this doesn't block anything happening right now (the app already exists, is already named this, already has real users), it's specifically about the moment real money and a bigger public push attach to the name.
6. **RESOLVED, as a recommendation.** Keep "reference engineering" as language describing the user's separate, already-existing project, and keep Kanvaz's own marketing/tagline language distinct from it (continuing to use its own established framing — "pre-production platform," "Reference Operating System," or whatever this planning round's eventual README rewrite lands on) rather than adopting "reference engineering" as Kanvaz's own category term. Avoids two related-but-separate things blending together in the same public materials. Recorded as the recommended default; not something the user explicitly confirmed word-for-word, so worth a quick nod or override when the actual README/marketing copy gets written.

## Standing constraints carried forward (v8.x)

Same discipline as every prior line: no native dependencies without a real audit first (a WASM module is not exempt from scrutiny just because it isn't a native N-API addon — verify its actual build provenance and maintenance status before vendoring, same bar Three.js and pdf.js were held to). 100% offline core stays non-negotiable regardless of monetization changes — a paid pack's distribution/licensing mechanism must not introduce a silent network dependency into the core app. Every release still gets CHANGELOG.md/docs/HANDOFF.md/SECURITY.md updated, and real live verification via CDP before calling anything done.

---

# The v7.x line's own standing constraints (historical, unchanged)

Same discipline as the whole v6.x arc: no native dependencies without a real audit first (`npm view <pkg> dependencies`, check for `.node` files) — Three.js and PDF.js both need this check confirmed at implementation time, not just assumed from their reputation as "pure JS" libraries. 100% offline core stays non-negotiable; none of the above touches the network. Every release still gets CHANGELOG.md/docs/HANDOFF.md/SECURITY.md updated, and real live verification (the CDP-driven technique from v6.6.1/6.6.2, not just a boot-test) before calling anything done — that lesson from this exact arc's own bugs stands as a hard requirement going forward, not a suggestion.

---

## Not a version, flagged so it doesn't get lost

~~The website update has been explicitly held off multiple times — release/CHANGELOG work first, website is a separate deliberate joint step. With development now closed out at v5.3.0, this is unscoped future work if the user chooses to pursue it — not part of the app's own release cadence.~~ **Superseded 2026-09-15** — see the v8.x line's item 6, above: a real GitHub Pages website is now a decided (though not started) post-v8 deliverable, not indefinitely unscoped.

Full per-plugin process isolation remains explicitly declined (multi-week rearchitect, not worth it against the current plugin ecosystem size) — logged as deliberate future work in `SECURITY.md`, not a gap anyone missed. With development closed out, this stays declined; it isn't something a GitHub issue against this repo should expect to reopen.

~~Free template gallery — raised mid-session, not yet scoped.~~ **Done, shipped in v5.1.0.** Scoped as an in-app "Start from Template" button (not a website/landing-page play) with 3 bundled starter boards — see `CHANGELOG.md`'s v5.1.0 entry.

---

## Settings — "extreme personalization and usability" pass (planned, not started)

Direct ask, after shipping the 3 grid styles: "even more settings which will
be there for extreme personalization and usability cases... plan it." This
is that plan — proposed additions to Settings, roughly in priority order.
Each one is scoped to build on something that already shipped, not a fresh
subsystem, so none of these should be a large lift individually.

1. **Custom accent color** — a color swatch in Settings → Appearance, using
   `colorpicker.js` (shipped this pass) to let the user pick their own
   accent instead of the fixed default. Every place that already reads
   `--color-accent` (grid tint, selection rings, buttons) picks it up for
   free; no per-feature work needed beyond writing the one CSS variable.
2. **Grid cell size** — a numeric field (or a slider, 8–64px) instead of
   only the 3 fixed style presets. Reference/Game Dev styles both already
   compute from a single `baseSpacing` constant — this just makes that
   constant user-settable per style, with 24px/32px kept as each style's
   default.
3. **Toolbar auto-hide sensitivity** — the reveal-zone height (currently a
   hardcoded 4px trigger band, see `app.js`'s `chromeEdgeMouseMove`) and
   the reveal/hide transition speeds (currently fixed at 0.12s/0.55s in
   `main.css`) as adjustable values, for anyone who found the current
   defaults too twitchy or too slow after this session's fixes.
4. **Default annotation color/width** — the annotation toolbar already
   remembers "last used" for a session (`activeColor`/`activeWidth` in
   `annotate.js`); persisting that as a real setting (survives a restart)
   rather than resetting to pen/red every launch is a small, real win for
   anyone with a consistent annotation style.
5. **Card corner radius** — `--radius-card` is currently one fixed value
   app-wide; exposing it as a slider (sharp/default/very rounded) is a
   pure CSS-variable change, same shape as the accent-color idea above.
6. **Reduced-motion / high-contrast toggles** — `animationsOn` already
   exists for the first; a high-contrast mode (boosted border/text
   contrast ratios) would be a genuinely new small CSS variant, useful
   for accessibility and for very bright work environments.
7. **Snap-to-grid strictness** — `gridSnapEnabled`/`gridSnapIncrement`
   already exist; adding a numeric "snap radius" (how close counts as
   "close enough to snap") gives finer control than the current binary
   on/off.

Deliberately NOT on this list: full keyboard-shortcut remapping (a real,
separate subsystem — worth its own scoping pass if the user wants it,
not a quick settings-panel addition) and a settings-only export/import
separate from full profile export (profile export already covers this;
a narrower "just settings" export would need its own decision about
whether that's actually a distinct enough use case to justify).
